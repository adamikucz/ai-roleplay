"use client";
import { FormEvent, useState } from "react";
import { SendHorizonal } from "lucide-react";
export function Composer({disabled,onSend}:{disabled:boolean;onSend:(content:string)=>void}){ const [v,setV]=useState(''); function submit(e:FormEvent){e.preventDefault(); const c=v.trim(); if(!c||disabled)return; setV(''); onSend(c);} return <form onSubmit={submit} className="border-t border-white/10 bg-black/20 p-3 backdrop-blur-xl md:p-4"><div className="flex items-end gap-3 rounded-[1.6rem] border border-white/10 bg-white/[.06] p-2"><textarea value={v} disabled={disabled} rows={1} onChange={e=>setV(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault(); submit(e);}}} placeholder="Napisz coś..." className="max-h-40 min-h-12 flex-1 resize-none bg-transparent px-3 py-3 text-sm leading-6 outline-none placeholder:text-white/35 disabled:opacity-50"/><button disabled={disabled||!v.trim()} className="grid h-12 w-12 place-items-center rounded-2xl bg-violet-500 shadow-lg transition hover:bg-violet-400 disabled:cursor-not-allowed disabled:opacity-40" aria-label="Send"><SendHorizonal className="h-5 w-5"/></button></div></form>; }
